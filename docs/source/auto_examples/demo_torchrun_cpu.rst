
.. DO NOT EDIT.
.. THIS FILE WAS AUTOMATICALLY GENERATED BY SPHINX-GALLERY.
.. TO MAKE CHANGES, EDIT THE SOURCE PYTHON FILE:
.. "auto_examples/demo_torchrun_cpu.py"
.. LINE NUMBERS ARE GIVEN BELOW.

.. only:: html

    .. note::
        :class: sphx-glr-download-link-note

        :ref:`Go to the end <sphx_glr_download_auto_examples_demo_torchrun_cpu.py>`
        to download the full example code.

.. rst-class:: sphx-glr-example-title

.. _sphx_glr_auto_examples_demo_torchrun_cpu.py:


Distributed Benchmark Run with torchrun (CPU)
==============================================

Load dataset and run benchmark with torchrun for distributed execution.

.. GENERATED FROM PYTHON SOURCE LINES 9-11

Setup
=====

.. GENERATED FROM PYTHON SOURCE LINES 11-41

.. code-block:: Python

    import os
    import sys
    import subprocess
    from pathlib import Path

    # Add repo root to path
    try:
        project_root = Path(__file__).resolve().parent.parent
    except NameError:
        # When running in Sphinx-Gallery or notebook without __file__
        cwd = Path.cwd()
        # Search upward for benchmark root (directory containing 'configs', 'datasets', 'solvers')
        current = cwd
        while current != current.parent:
            if (current / 'configs').exists() and (current / 'datasets').exists():
                project_root = current
                break
            current = current.parent
        else:
            # Fallback: use cwd
            project_root = cwd

    sys.path.insert(0, str(project_root))

    from benchopt import config as bench_config
    from benchopt.config import yaml
    bench_config.get_data_path = lambda key=None: str(project_root / "data")










.. GENERATED FROM PYTHON SOURCE LINES 42-45

Load Configuration
==================
Load config from YAML file

.. GENERATED FROM PYTHON SOURCE LINES 45-56

.. code-block:: Python

    config_path = project_root / 'configs' / 'highres_imaging_torchrun.yml'
    print(f"\nLoading config from: {config_path.name}")
    with open(config_path, 'r') as f:
        config_content = yaml.safe_load(f)

    # Extract dataset parameters
    dataset_params = config_content['dataset'][0]['highres_color_image']
    print(f"\nDataset configuration:")
    for key, val in dataset_params.items():
        print(f"  - {key}: {val}")





.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Loading config from: highres_imaging_torchrun.yml

    Dataset configuration:
      - image_size: 256
      - num_operators: 4
      - noise_level: 0.1




.. GENERATED FROM PYTHON SOURCE LINES 57-59

Load Dataset
============

.. GENERATED FROM PYTHON SOURCE LINES 59-69

.. code-block:: Python

    from datasets.highres_color_image import Dataset
    print("\nLoading dataset...")
    ds = Dataset(**dataset_params, seed=42)
    data = ds.get_data()

    print(f"\nDataset loaded successfully:")
    print(f"  - Ground truth shape: {data['ground_truth'].shape}")
    print(f"  - Measurements: {len(data['measurement'])} operators")
    print(f"  - First measurement shape: {data['measurement'][0].shape}")





.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Loading dataset...
    submitit not installed, dataset will run in non-distributed mode
    DistributedContext: rank 0 / 1
    Using cached CBSD_0010.png from /local/home/bm284012/Workspace/deepinv_benchmark/benchmark_invprob_inference/data
    Resized image from (481, 321) to (256, 170)
    Debug figure saved to debug_output/highres_color_image_measurements.png

    Dataset loaded successfully:
      - Ground truth shape: torch.Size([1, 3, 256, 170])
      - Measurements: 4 operators
      - First measurement shape: torch.Size([1, 3, 256, 170])




.. GENERATED FROM PYTHON SOURCE LINES 70-73

Visualize Dataset
=================
Optional: visualize ground truth and measurements

.. GENERATED FROM PYTHON SOURCE LINES 73-86

.. code-block:: Python

    try:
        from deepinv.utils.plotting import plot
    
        gt = data['ground_truth']
        meas = data['measurement'][0]  # First measurement

        print("\nGenerating preview plot...")
        plot([gt, meas], titles=['Ground Truth', 'Measurement 1'])
    except Exception as e:
        print(f"\nNote: Skipping visualization ({type(e).__name__})")






.. image-sg:: /auto_examples/images/sphx_glr_demo_torchrun_cpu_001.png
   :alt: Ground Truth, Measurement 1
   :srcset: /auto_examples/images/sphx_glr_demo_torchrun_cpu_001.png
   :class: sphx-glr-single-img


.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    Generating preview plot...




.. GENERATED FROM PYTHON SOURCE LINES 87-90

Run Benchmark
=============
To run the distributed benchmark with torchrun, execute the command below.

.. GENERATED FROM PYTHON SOURCE LINES 90-104

.. code-block:: Python


    print("\n" + "-" * 70)
    print("To run the distributed benchmark with torchrun, execute:")
    print("-" * 70)
    cmd = [
        'benchopt', 'run', '.',
        '--parallel-config', './configs/torchrun_config.yml',
        '--config', './configs/highres_imaging_torchrun.yml'
    ]
    print(f"  {' '.join(cmd)}")
    print("-" * 70)

    # Uncomment to run:
    # subprocess.run(cmd, check=True)




.. rst-class:: sphx-glr-script-out

 .. code-block:: none


    ----------------------------------------------------------------------
    To run the distributed benchmark with torchrun, execute:
    ----------------------------------------------------------------------
      benchopt run . --parallel-config ./configs/torchrun_config.yml --config ./configs/highres_imaging_torchrun.yml
    ----------------------------------------------------------------------





.. rst-class:: sphx-glr-timing

   **Total running time of the script:** (0 minutes 1.109 seconds)


.. _sphx_glr_download_auto_examples_demo_torchrun_cpu.py:

.. only:: html

  .. container:: sphx-glr-footer sphx-glr-footer-example

    .. container:: sphx-glr-download sphx-glr-download-jupyter

      :download:`Download Jupyter notebook: demo_torchrun_cpu.ipynb <demo_torchrun_cpu.ipynb>`

    .. container:: sphx-glr-download sphx-glr-download-python

      :download:`Download Python source code: demo_torchrun_cpu.py <demo_torchrun_cpu.py>`

    .. container:: sphx-glr-download sphx-glr-download-zip

      :download:`Download zipped: demo_torchrun_cpu.zip <demo_torchrun_cpu.zip>`


.. only:: html

 .. rst-class:: sphx-glr-signature

    `Gallery generated by Sphinx-Gallery <https://sphinx-gallery.github.io>`_
