# Benchopt configuration for distributed benchmarking with Urban100
# This file specifies testing with 2 distributed processes

# Objective filter
objective:
  - reconstruction_objective

# Dataset filters
dataset:
  - urban100:
      image_size: 512
      num_operators: 4
      train_images: 64
      val_images: 16
      batch_size: 1
      noise_level: 0.1


# Solver filters
solver:
  - UnrolledPnP:
      denoiser: drunet
      n_iter: 4
      init_stepsize: 0.9
      denoiser_sigma: 0.05
      learning_rate: 1e-4
      model_learning_rate: 1e-4
      slurm_gres, slurm_ntasks_per_node, slurm_nodes, distribute_model, patch_size, overlap, max_batch_size: [
        ["gpu:1", 1, 1, false, 0, 0, 0],
        ["gpu:2", 2, 1, true, 256, 32, 4],
        ["gpu:4", 4, 1, true, 256, 32, 4],
      ]

# Run parameters
max-runs: 5
n-repetitions: 1
